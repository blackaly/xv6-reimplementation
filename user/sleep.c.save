#include <stdio.h> include <stdlib.h> include <string.h>
int main(int argc, char* argv[]){

	if(argc < 2){
		printf("No argument provided");
		exit(0);
	}
	int ticks = atoi(argv[1]);
	int counter = 0;
	while(counter < ticks) counter++;

	return 0;
}
